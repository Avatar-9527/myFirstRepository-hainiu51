1、了解类加载器
    双亲委派机制

    加载 链接（验证 准备 解析） 初始化

    类加载器加载系统资源

2、了解反射机制

3、获取Class对象的三种方式
    Class.forName();
    数据类型.class
    对象.getClass()

4、反射获取类中的成员并使用
    构造方法 -- 创建对象
    成员变量 -- 赋值 获取值
    成员方法 -- 执行

    注意：成员没有权限访问，暴力反射

5、能够说出枚举的作用 -- 表示一组常量
    掌握枚举的使用  枚举类.枚举项

6、掌握注解的使用


作业：
1、集合HashMap排序

2、反射
    读取配置文件 动态创建对象
    越过泛型的编译期检查

3、单例设计模式